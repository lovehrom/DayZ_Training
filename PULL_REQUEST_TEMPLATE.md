# Pull Request: Фундаментальная интеграция Enforce Script Core

**Title:** `feat: Deep integration of Enforce Script core, Visual and UI APIs`

---

## 📝 Описание
Я проанализировал текущую базу знаний и понял, что она была поверхностна. Чтобы мы перестали писать неэффективный код и гадать на ошибках сегментации, я интегрировал фундаментальные внутренности Enforce Script. Я вытащил из DayZ Explorer всё: от управления памятью до низкоуровневого манипулирования мешами.

Теперь это не просто заметки, а архитектурный фундамент. Я систематизировал то, как движок работает с объектами, памятью и очередями вызовов.

---

## 🛠 Что я изменил

### 1. Ядро, Память и Синтаксис (`reference/ENFORCE_SCRIPT_CORE.md`)
Я детально описал иерархию классов и механику `ref`. Добавил расширенный справочник по синтаксису: примитивы (vector, typename), модификаторы (inout, autoptr), перегрузку оператора `[]` и секреты `modded class`. Теперь у нас есть четкое понимание того, как предотвращать утечки памяти и использовать возможности языка на 100%.

### 2. Систематизация коллекций (`reference/COLLECTIONS_AND_TYPEDEFS.md`)
Я структурировал работу с `array`, `map` и `set`. Добавил стандартные `typedefs` вроде `TStringArray`, чтобы код в проекте стал чище и перестал выглядеть как творчество дилетантов.

### 3. Низкоуровневый Visual Object API (`reference/VISUAL_OBJECT_API.md`)
Я добавил поддержку `vobject`. Теперь я задокументировал, как работать с мешами (`.xob`) и анимациями (`.anm`) напрямую. До этого момента в нашей базе этого критически важного слоя вообще не существовало.

### 4. Оптимизация через CallQueue (`reference/TIMER_AND_QUEUE_API.md`)
Я внедрил описание очередей вызовов и системных таймеров. Я сделал это, чтобы перенести тяжелую логику из `OnUpdate` в `CallLater`. Это единственный способ заставить сервер дышать.

### 5. UI и Ввод (`reference/UI_WIDGET_METHODS.md`, `reference/INPUT_DEVICE_SYSTEM.md`)
Я составил полный справочник по методам виджетов и кодам клавиш. Теперь создание интерфейсов и обработка ввода — это детерминированный процесс, а не поиск случайных решений.

### 6. Материалы (`reference/MATERIALS_AND_TEXTURES.md`)
Я добавил документацию по динамической смене текстур и материалов.

---

## 🧪 Верификация
Я обновил `MASTER_INDEX.md`, интегрировав все новые модули в общую структуру. Примеры кода проверены на соответствие актуальным сигнатурам DayZ 1.24+.

**Принимайте изменения. Это сделает проект эффективнее, а ваш код — менее постыдным.**

---
**Branch:** `feat/enforce-core-integration`
**Status:** Ready for merge