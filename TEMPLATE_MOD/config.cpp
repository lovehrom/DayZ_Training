// =================================================================
// TEMPLATE MOD - Готовый шаблон для вашего мода
// =================================================================
// Инструкция:
// 1. Замените "TEMPLATE_MOD" на название вашего мода
// 2. Замените "TemplateItem" на название вашего предмета
// 3. Отредактируйте свойства в CfgVehicles
// 4. Создайте скрипт предмета в scripts/3_Game/Items/
// =================================================================

class CfgPatches
{
    class TEMPLATE_MOD
    {
        // ↑ ЗАМЕНИТЕ на название мода (без пробелов)

        units[] = {"TemplateItem"};
        // ↑ ЗАМЕНИТЕ на список ваших предметов

        weapons[] = {};
        requiredVersion = 0.1;
        requiredAddons[] = {
            "DZ_Data",
            "DZ_Items"
        };
    };
};

class CfgVehicles
{
    class ItemBase;

    class TemplateItem: ItemBase
    {
        // ↑ ЗАМЕНИТЕ "TemplateItem" на название вашего предмета

        scope = 2;  // 0 = скрыт, 1 = наследуется, 2 = публичный
        displayName = "Template Item";  // Название предмета в игре
        description = "Описание вашего предмета";  // Описание

        // Модель (используйте заглушку для тестов)
        model = "\DZ\items\apple.p3d";
        // ↑ Замените на вашу модель: "\TEMPLATE_MOD\models\my_item.p3d"

        // Вес и размер
        weight = 100;  // Вес в граммах (0.1 кг)
        itemSize[] = {1, 1};  // Размер в инвентаре (1x1)

        // Экономика (спавн)
        nominal = 100;  // Желаемое количество на сервере
        min = 50;       // Минимальное количество
        max = 150;      // Максимальное количество
        restock = 0;    // Время респавна (0 = не респавнится)
        lifetime = 7200;  // Время жизни (секунды)
        quantity = 1;  // Количество в стаке
    };
};
