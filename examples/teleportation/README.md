# Simple Teleportation System Example

**Статус:** ✅ Рабочий пример
**Сложность:** Средняя
**Время создания:** 20 минут

---

## 📋 Что это делает

Система телепортации для DayZ:
- JSON конфигурация точек телепортации
- Команда чата `/tp <location>`
- Автоматическая загрузка из $profile:
- Проверка безопасности (не в транспорте, не в combате)

---

## 🎯 Как использовать

### Step 1: Скопируйте файлы

```
examples/teleportation/
├── config.cpp           ← В ваш проект/config.cpp
├── teleports.json       ← В $profile:/teleports.json
└── scripts/
    └── 4_World/
        └── Classes/
            └── PlayerBase/
                └── playerbase_teleport.c  ← В ваш проект
```

### Step 2: Создайте JSON конфигурацию

Создайте файл `$profile:/teleports.json`:

```json
[
    {
        "Name": "Cherno",
        "Position": [6500, 0, 2500],
        "Description": "Chernogorsk"
    },
    {
        "Name": "Electro",
        "Position": [5000, 0, 2500],
        "Description": "Elektrozavodsk"
    },
    {
        "Name": "NWAF",
        "Position": [4500, 0, 8500],
        "Description": "Northwest Airfield"
    }
]
```

### Step 3: Запустите сервер

В чате напишите: `/tp Cherno`

---

## 📁 Структура файлов

```
teleportation/
├── README.md                        ← Этот файл
├── config.cpp                       ← Конфигурация мода
├── teleports.json                   ← Пример конфигурации
└── scripts/
    └── 4_World/
        └── Classes/
            ├── TeleportNode.c       ← Класс точки телепортации
            ├── TeleportManager.c    ← Менеджер телепортации
            └── PlayerBase/
                └── playerbase_teleport.c  ← Интеграция в PlayerBase
```

---

## 🔧 Как это работает

### 1. TeleportNode.c

Класс точки телепортации с:
- Названием
- Позицией (x, z, y)
- Описанием

### 2. TeleportManager.c

Менеджер который:
- Загружает точки из JSON
- Обрабатывает команду `/tp`
- Проверяет безопасность

### 3. playerbase_teleport.c

Интеграция в PlayerBase:
- Перехватывает чат
- Вызывает телепортацию

---

## 💡 Кастомизация

### Добавить новую точку:

**В teleports.json:**
```json
{
    "Name": "MyBase",
    "Position": [3000, 0, 3000],
    "Description": "My secret base"
}
```

### Изменить команду:

**В playerbase_teleport.c:**
```c
if (message.StartsWith("/tp ")) // Измените на "/teleport "
```

### Добавить cooldown:

**В TeleportManager.c:**
```c
protected float m_LastTeleportTime;
protected const float TELEPORT_COOLDOWN = 300; // 5 минут

bool CanTeleport(PlayerBase player)
{
    float currentTime = GetGame().GetTickTime();

    if (currentTime - m_LastTeleportTime < TELEPORT_COOLDOWN)
    {
        NotificationSystem.Notify(player, "Teleport on cooldown!");
        return false;
    }

    return true;
}
```

---

## ⚠️ Совместимость

- ✅ Работает с vanilla DayZ
- ✅ Работает с большинством модов
- ⚠️ Может конфликтовать с другими teleport модами

---

## 🐛 Troubleshooting

### Телепортация не работает

1. Проверьте что JSON файл существует
2. Проверьте формат JSON (валидация: https://jsonlint.com/)
3. Проверьте script log

### Ошибка "Location not found"

1. Проверьте название точки в JSON
2. Название чувствительно к регистру
3. Используйте точное название

---

## 📊 Производительность

- **Memory:** ~5 KB (загрузка при старте)
- **Disk I/O:** Раз при запуске сервера
- **Network:** Не влияет

---

## 🚀 Дальнейшее развитие

### Ideas:
1. **Cost** - стоимость телепортации (валюта)
2. **Cooldown** - задержка между использования
3. **Permissions** - права доступа
4. **GUI** - меню телепортации

**Смотрите:** [topics/13_teleportation.md](../../topics/13_teleportation.md)

---

*Последнее обновление: 2025-01-17*
*Пример создан для обучения*
