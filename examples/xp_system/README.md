# Simple XP System Example

**Статус:** ✅ Рабочий пример
**Сложность:** Средняя
**Время создания:** 30 минут

---

## 📋 Что это делает

Простая система опыта (XP) для DayZ:
- XP за убийство зомби (10 XP)
- XP за чтение книг (1500 XP)
- XP за крафтинг (500 XP)
- 5 уровней с бонусами
- Автосохранение в JSON

---

## 🎯 Как использовать

### Step 1: Скопируйте файлы

```
examples/xp_system/
├── config.cpp           ← В ваш проект/config.cpp
└── scripts/
    └── 4_World/
        └── Classes/
            └── PlayerBase/  ← В ваш проект/scripts/4_World/Classes/PlayerBase/
```

### Step 2: Добавьте в config.cpp

```cpp
#include "scripts/4_World/Classes/PlayerBase/playerbase_xp.c"
```

### Step 3: Запустите сервер

После убийства первого зомби вы получите 10 XP и увидите сообщение в чате.

---

## 📁 Структура файлов

```
xp_system/
├── README.md                    ← Этот файл
├── config.cpp                   ← Конфигурация мода
└── scripts/
    └── 4_World/
        └── Classes/
            ├── SimpleSkill.c           ← Класс навыка
            ├── SkillManager.c          ← Менеджер навыков
            └── PlayerBase/
                └── playerbase_xp.c     ← Интеграция в PlayerBase
```

---

## 🔧 Как это работает

### 1. SimpleSkill.c

Класс навыка с:
- Текущий опыт
- Уровень
- Автоматическим level-up

### 2. SkillManager.c

Менеджер который:
- Управляет 3 навыками (Survival, Crafting, Combat)
- Сохраняет в JSON
- Загружает из JSON

### 3. playerbase_xp.c

Интеграция в PlayerBase:
- Дает XP за убийство зомби
- Дает XP за чтение книг
- Дает XP за крафтинг
- Автосохранение при дисконнекте

---

## 💡 Кастомизация

### Изменить значения XP:

**В SkillManager.c:**
```c
// По умолчанию: 10 XP за зомби
void OnZombieKilled()
{
    m_Skills.Get("Combat").AddXP(10); // Измените это значение
}
```

### Добавить новый навык:

**В SkillManager.c:**
```c
void SkillManager()
{
    m_Skills.Insert("Survival", new SimpleSkill("Survival"));
    m_Skills.Insert("Crafting", new SimpleSkill("Crafting"));
    m_Skills.Insert("Combat", new SimpleSkill("Combat"));
    m_Skills.Insert("Mining", new SimpleSkill("Mining")); // <-- НОВЫЙ
}
```

### Изменить требуемый XP:

**В SimpleSkill.c:**
```c
void CheckLevelUp()
{
    int requiredXP = Level * 1000; // По умолчанию 1000 XP per level
    // Измените на Level * 500 для более быстрого leveling
}
```

---

## ⚠️ Совместимость

- ✅ Работает с vanilla DayZ
- ✅ Работает с большинством модов
- ⚠️ Может конфликтовать с другими XP модами

---

## 🐛 Troubleshooting

### XP не сохраняется

Проверьте права на запись `$profile:`:
```bash
# Windows
# Убедитесь что сервер имеет права на запись
```

### Ошибки в script log

Проверьте:
1. Правильность путей в config.cpp
2. Файлы скопированы в правильные папки
3. Script log: `scripts.log`

---

## 📊 Производительность

- **Memory:** ~1 KB per player
- **Disk I/O:** Раз в 5 минут (при дисконнекте)
- **Network:** Не влияет (client-side notifications только)

---

## 🚀 Дальнейшее развитие

### Ideas:
1. **Perk system** - бонусы за уровни
2. **GUI** - меню навыков
3. **Leaderboard** - топ игроков
4. **Achievements** - достижения

**Смотрите:** [topics/12_xp_systems.md](../../topics/12_xp_systems.md)

---

*Последнее обновление: 2025-01-17*
*Пример создан для обучения*
