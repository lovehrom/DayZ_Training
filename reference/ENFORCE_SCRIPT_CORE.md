# ENFORCE_SCRIPT_CORE - Базовые концепции языка

**Назначение:** Описание фундаментальных классов, типов и механизмов Enforce Script (DayZ 1.24+).
**Источник:** Dayz Explorer (Enforce script essentials)

---

## 🏗️ Иерархия базовых классов

### Class
Супер-корень всех классов в Enforce Script. Все объекты в языке являются производными от этого класса.
- **Особенности:** Не имеет автоматического управления памятью (требует ручного контроля, если не обернуто в `ref`).

### Managed
Базовый класс для объектов с "управляемой" памятью. 
- **Особенности:** Большинство высокоуровневых классов DayZ (EntityAI, PlayerBase) наследуются от `Managed` через длинную цепочку.

### vobject
Низкоуровневый тип данных для визуальных ресурсов (меши .xob, анимации .anm).
- **Подробнее:** См. [VISUAL_OBJECT_API.md](VISUAL_OBJECT_API.md).

### ScriptModule
Класс, представляющий скомпилированный модуль скриптов.
- **Использование:** Позволяет загружать и вызывать функции из разных скриптовых уровней (Game, World, Mission).

---

## 📋 Глобальные переменные и константы

| Переменная | Тип | Описание |
|------------|-----|----------|
| `ftime` | `float` | DeltaTime с последнего кадра. Используется для плавной анимации и вычислений в `OnUpdate`. |
| `FLT_MAX` | `float` | Максимальное значение для числа с плавающей запятой. |
| `FLT_MIN` | `float` | Минимальное значение для числа с плавающей запятой. |

---

## 🛠️ Системные функции (EnScript)

Класс `EnScript` предоставляет статические утилиты для работы с движком.

### GetClassType(Class obj)
Возвращает тип объекта для RTTI (Run-Time Type Information).

---

## 🧠 Управление памятью: Ключевое слово `ref`

В Enforce Script ключевое слово `ref` (Reference) управляет жизненным циклом объекта.

- **`ref Class myObj = new Class();`**
  Объект будет удален автоматически, когда на него не останется ссылок (strong references).
- **`Class myObj;`**
  Слабая ссылка (weak reference). Если объект, на который она указывает, будет удален, ссылка станет `null`.

### Правила:
1. Используйте `ref` для переменных-членов класса, если этот класс "владеет" объектом.
2. Не используйте `ref` для локальных переменных функций, если вы не создаете новый объект внутри.

---

## 🧵 Потоки и выполнение (Threading)

Enforce Script поддерживает концепцию потоков (чаще всего это микро-потоки или фиберы).

| Функция | Описание |
|---------|----------|
| `Idle()` | Приостанавливает выполнение текущего потока и передает управление другим. (Устарело, но встречается). |
| `KillThread(Class owner, string name)` | Останавливает поток по имени первой функции в стеке. |
| `ThreadFunction(...)` | Debug-функция для получения текущей функции в стеке потока. |

---

## 🔍 Полезные советы
- Всегда проверяйте объекты на `null` перед вызовом методов, так как Enforce Script не защищен от Null Pointer Exception.
- Используйте `ftime` для любых расчетов, зависящих от времени, чтобы логика не зависела от FPS сервера/клиента.
