# INPUT DEVICE SYSTEM - Управление и ввод

**Назначение:** Описание работы с устройствами ввода (клавиатура, мышь, геймпад).
**Источник:** DayZ Explorer (InputDevice Group)

---

## ⌨️ Клавиатура и Мышь

В DayZ ввод обрабатывается через систему `Input`. Большинство кастомных модов используют переопределение методов в классах `Mission` или `UIScriptedMenu`.

### Основные методы проверки (в UI или Mission):
- **KeyPress(int key)**: Срабатывает при нажатии клавиши.
- **MousePress(int button)**: Срабатывает при нажатии кнопки мыши.

---

## 🆔 Коды клавиш (Key Codes)

DayZ использует стандартные скан-коды (DIK Codes).

| Клавиша | Константа (примерная) | DIK Код |
|---------|-----------------------|---------|
| **ESC** | `KeyCode.Escape` | 0x01 |
| **F1** | `KeyCode.F1` | 0x3B |
| **TAB** | `KeyCode.Tab` | 0x0F |
| **L-SHIFT**| `KeyCode.LShift` | 0x2A |
| **SPACE** | `KeyCode.Space` | 0x39 |

*Примечание: Полный список DIK-кодов можно найти в документации Enfusion или стандартных заголовочных файлах скриптов.*

---

## 🎮 Геймпад и джойстики

Для работы с геймпадами используются специальные константы осей и кнопок:
- `UAUILeft`, `UAUIRight`, `UAUIUp`, `UAUIDown` — для навигации в меню.
- `UAAction` — основное действие.

---

## 🛠️ Работа с профилем игрока

Функции для чтения и записи настроек пользователя:

### GetProfileValueBool(string name)
Читает логическое значение из профиля игрока.
- **Пример:** `bool isHintsEnabled = GetGame().GetProfileValueBool("TutorialHints");`

### SetProfileValueBool(string name, bool value)
Записывает значение в профиль.

---

## 🖱️ Управление курсором

### GetMousePos(out int x, out int y)
Возвращает текущие координаты курсора на экране.

### GetMouseState(MouseState state)
Позволяет узнать состояние кнопок и колеса прокрутки.

---

## 🔍 Советы
- **Input Filtering:** Если вы создаете меню, не забывайте блокировать ввод в игру через `GetGame().GetInput().ChangeGameFocus(1)`, чтобы игрок не стрелял, пока лазит в инвентаре.
- **User Actions:** Для игровых действий лучше использовать систему `User Action`, а не прямую проверку клавиш, чтобы игроки могли переназначить кнопки в настройках.
