# MATERIALS AND TEXTURES - Визуальные эффекты

**Назначение:** Описание работы с материалами (.rvmat), текстурами и цветами.
**Источник:** DayZ Explorer (Materials Group)

---

## 🎨 Работа с цветом

В DayZ цвета обычно задаются в формате ARGB (Alpha, Red, Green, Blue) в виде целого числа (`int`).

### GetTemperatureColor(float temperature)
Специальная функция движка, возвращающая цвет (ARGB), соответствующий физической температуре. Полезно для визуализации нагретых предметов (стволы оружия, печи).

---

## 💎 Материалы (.rvmat)

Материалы определяют, как поверхность объекта взаимодействует со светом (блики, тени, текстуры нормалей).

### Динамическая смена материалов
Через скрипты можно менять материалы на определенных слотах меша:
- **`SetObjectMaterial(int selection_index, string material_path)`**
  - `selection_index`: Индекс секции модели (из конфига).
  - `material_path`: Путь к `.rvmat` файлу.

---

## 🖼️ Текстуры (.paa / .edds)

Текстуры — это основные изображения, накладываемые на модель.

### Смена текстур в скрипте
- **`SetObjectTexture(int selection_index, string texture_path)`**
  - Позволяет динамически "перекрашивать" объекты.
  - **Пример:** Смена скина автомобиля или одежды.

---

## 🌡️ Эффекты материалов
Движок поддерживает встроенные эффекты, такие как:
- **Намокание (Wetness):** Визуально делает поверхность темнее и блестящее.
- **Повреждение (Damage):** Накладывает текстуру царапин или грязи.

---

## 🔍 Советы
- **Оптимизация:** Смена текстур или материалов — это операция, которая заставляет движок перерисовывать объект. Не делайте этого слишком часто.
- **Selection Names:** Чтобы узнать `selection_index`, нужно смотреть в `.p3d` файл модели (через Object Builder) или искать `hiddenSelections` в `config.cpp` ванильных предметов.
