# STRINGS AND PARSING - Работа со строками

**Назначение:** Описание функций для манипуляции текстом и парсинга токенов.
**Источник:** Dayz Explorer (Enforce script essentials)

---

## 🛠️ Базовые функции строк

### String(string s)
Вспомогательная функция для передачи строкового выражения в функции с параметром `void`.
- **Пример:** `Print(String("Value: " + myVar));`

### PrintString(string s)
Печатает строковое выражение в консоль/лог.
- **Пример:** `PrintString("Debug: " + myVar);`

---

## 🔍 Парсинг строк (Tokens)

Enforce Script предоставляет мощные средства для разбиения строки на токены. Это полезно при чтении кастомных конфигов или команд чата.

### ParseString(string input, out string tokens[])
Разбивает строку на токены (по пробелам) и возвращает количество найденных токенов.
- **Пример:**
```c
string tokens[2];
int count = ParseString("Hello World", tokens); // count = 2, tokens = {"Hello", "World"}
```

### ParseStringEx(inout string input, out string token)
Более продвинутый парсинг. Извлекает **один** токен из начала строки и возвращает его **тип**. Входная строка при этом укорачивается (левая часть удаляется).

#### Типы возвращаемых токенов:
| ID | Тип | Описание |
|----|-----|----------|
| **0** | Error | Токен не найден или ошибка. |
| **1** | Defined | Специальные символы ( . / * и т.д. ). |
| **2** | Quoted | Строка в кавычках (кавычки удаляются). |
| **3** | Alpha | Буквенная строка. |
| **4** | Number | Числовое значение. |
| **5** | EOL | Конец строки. |

#### Пример использования:
```c
string input = "Hello*123";
string token;
int type;

type = ParseStringEx(input, token); // type = 3 (Alpha), token = "Hello", input = "*123"
type = ParseStringEx(input, token); // type = 1 (Defined), token = "*", input = "123"
type = ParseStringEx(input, token); // type = 4 (Number), token = "123", input = ""
```

---

## 🧵 Полезные паттерны

### Проверка на пустоту
Всегда проверяйте строку перед парсингом, чтобы избежать бесконечных циклов.
```c
if (myString != "") {
    // выполнение логики
}
```

### Конвертация типов
Хотя эти функции не описаны в Essentials HTML, они являются частью базового API:
- `myString.ToInt()` — в целое число.
- `myString.ToFloat()` — в число с плавающей точкой.
- `myString.ToVector()` — в вектор (формат "x y z").
- `myInt.ToString()` — в строку.
