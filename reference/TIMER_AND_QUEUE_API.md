# TIMER AND CALL QUEUE API - Отложенные вызовы и таймеры

**Назначение:** Описание механизмов для выполнения кода по расписанию, через интервалы или в следующем кадре.
**Источник:** DayZ Explorer (Tools Group)

---

## 📅 CallQueue (Очередь вызовов)

`CallQueue` позволяет запланировать выполнение функции. Это более эффективно, чем проверка условий в `OnUpdate`.

### GetGame().GetCallQueue(int category)
Возвращает очередь вызовов для определенной категории (обычно `CALL_CATEGORY_SYSTEM` или `CALL_CATEGORY_GAMEPLAY`).

### Call(Class obj, string function_name, Param params = NULL)
Планирует вызов функции в **следующем** кадре.
- **Пример:**
```c
GetGame().GetCallQueue(CALL_CATEGORY_GAMEPLAY).Call(this, "MyDelayedFunction", new Param1<string>("Hello"));
```

### CallLater(Class obj, string function_name, int delay, bool repeat, Param params = NULL)
Планирует вызов функции через определенное время (в миллисекундах).
- **Параметры:**
  - `delay`: Задержка в мс.
  - `repeat`: Если `true`, функция будет вызываться циклически.
- **Пример:**
```c
GetGame().GetCallQueue(CALL_CATEGORY_GAMEPLAY).CallLater(this, "MyRepeatingFunction", 5000, true);
```

### Remove(Class obj, string function_name)
Удаляет запланированный вызов. Обязательно вызывайте это в деструкторах, чтобы избежать вызовов на удаленных объектах!

---

## ⏱️ Таймеры (TimerBase)

Таймеры — это объекты, которые управляют собственным циклом выполнения.

### Основные типы:
1. **Timer**: Обычный таймер для логики.
2. **AnimationTimer**: Таймер, оптимизированный для плавных изменений значений (интерполяция).
3. **AnimatorTimer**: Используется для управления состояниями анимации.

### Методы Timer:
| Метод | Описание |
|-------|----------|
| `Run(float time, Managed obj, string fn, Param params, bool loop)` | Запустить таймер. |
| `Stop()` | Остановить таймер. |
| `Pause()` / `Continue()` | Пауза и продолжение. |
| `IsRunning()` | Возвращает `true`, если таймер активен. |
| `GetRemaining()` | Сколько времени осталось до срабатывания. |

---

## 🖱️ DragQueue
Специальная очередь для обработки операций Drag & Drop. Позволяет плавно отслеживать перемещение объектов в UI.

---

## 🧩 Параметры (Param)
Для передачи данных в очереди и таймеры используются классы `Param`:
- `Param1<type1>`
- `Param2<type1, type2>`
- ... вплоть до `Param10`.

---

## 🔍 Советы
- **Безопасность:** Всегда проверяйте, существует ли объект, перед выполнением логики в отложенной функции.
- **Оптимизация:** Используйте `CallLater` вместо `OnUpdate`, если вам не нужна проверка каждый кадр (например, проверка голода игрока раз в 10 секунд).
