# UI WIDGET METHODS - Детальный справочник методов

**Назначение:** Полный список методов для управления различными типами виджетов в DayZ.
**Источник:** DayZ Explorer (UI Group)

---

## 🔘 Базовые методы Widget
Эти методы доступны почти для всех типов виджетов.

| Метод | Описание |
|-------|----------|
| `Show(bool show)` | Показать или скрыть виджет. |
| `Enable(bool enable)` | Сделать виджет активным или серым (неактивным). |
| `SetFocus()` | Установить фокус ввода на этот виджет. |
| `GetParent()` | Возвращает родительский виджет. |
| `SetAlpha(float alpha)` | Установить прозрачность (0.0 - 1.0). |
| `SetColor(int color)` | Установить цвет виджета (ARGB). |

---

## 🖱️ Кнопки (ButtonWidget)

| Метод | Описание |
|-------|----------|
| `SetText(string text)` | Установить текст на кнопке. |
| `GetText()` | Получить текущий текст кнопки. |
| `SetState(bool state)` | Установить состояние кнопки (нажата/отжата). |

---

## 📝 Текстовые поля и Ввод (EditBoxWidget, TextWidget)

| Метод | Описание |
|-------|----------|
| `SetText(string text)` | Изменить содержимое текста. |
| `GetText()` | Получить текст из поля ввода. |
| `SetLimit(int limit)` | Ограничить количество символов для ввода. |
| `Clear()` | Очистить поле. |

---

## 🎚️ Слайдеры и Прогресс (SliderWidget, ProgressBarWidget)

| Метод | Описание |
|-------|----------|
| `SetCurrent(float value)`| Установить текущее значение. |
| `GetCurrent()` | Получить текущее значение. |
| `SetMax(float max)` | Установить максимальный предел. |
| `SetMin(float min)` | Установить минимальный предел. |
| `SetStep(float step)` | Установить шаг изменения значения. |

---

## 📂 Списки (TextListboxWidget)

| Метод | Описание |
|-------|----------|
| `AddItem(string text, Managed data, int column)` | Добавить строку в список. |
| `RemoveRow(int row)` | Удалить строку по индексу. |
| `GetSelectedRow()` | Возвращает индекс выбранной строки (-1, если ничего не выбрано). |
| `SetSelectedRow(int row)`| Выбрать строку программно. |
| `ClearItems()` | Полная очистка списка. |

---

## 🖼️ Изображения (ImageWidget)

| Метод | Описание |
|-------|----------|
| `LoadImageFile(int image, string path)` | Загрузить изображение в указанный слот. |
| `SetImage(int image)` | Переключиться на загруженное изображение. |
| `SetRotation(float deg)` | Повернуть изображение на указанный угол. |

---

## 💡 Советы по UI
- **Слои:** Используйте `GetRootWidget().AddChild(...)` для правильной иерархии.
- **События:** Большинство действий (нажатие кнопки, изменение слайдера) обрабатываются через `OnClick`, `OnMouseButtonDown` или `OnChange` в вашем контроллере меню.
- **Производительность:** Не вызывайте `SetText` или `Show/Hide` в каждом кадре `OnUpdate`, если данные не изменились.
