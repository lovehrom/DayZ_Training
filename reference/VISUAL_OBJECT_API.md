# VISUAL OBJECT API - Работа с 3D-объектами и мешами

**Назначение:** Описание функций для работы с визуальными объектами (vobject, XOB) и анимациями на низком уровне.
**Источник:** DayZ Explorer (Visual Group)

---

## 🧊 Понятие vobject
`vobject` — это внутренний тип данных Enforce Script, представляющий визуальный ресурс (модель .xob или анимацию). Он используется для прямого управления ресурсами движка.

---

## 🛠️ Функции управления объектами

### GetObject(string name)
Загружает визуальный объект (меш или анимацию) по его имени/пути или получает его из кэша, если он уже загружен.
- **Возвращает:** `vobject` (указатель на ресурс).
- **Пример:**
```c
vobject myMesh = GetObject("MyMod/data/models/item.xob");
```

### ReleaseObject(vobject object, int flag = 0)
Освобождает визуальный объект. Если на объект больше нет ссылок, он может быть выгружен из памяти или оставлен в кэше (в зависимости от флагов).
- **Параметры:**
  - `object`: Объект для освобождения.
  - `flag`: Флаги управления освобождением (обычно 0).

### vtoa(vobject vobj)
Возвращает имя (путь) визуального объекта в виде строки. Полезно для отладки или сравнения ресурсов.
- **Пример:**
```c
string path = vtoa(myMesh);
Print(path); // Выведет путь к .xob файлу
```

---

## 🏃 Работа с анимациями

Если `vobject` является анимационным файлом, можно использовать следующие функции:

### GetNumAnimFrames(vobject anim)
Возвращает общее количество кадров в анимационном объекте.
- **Пример:**
```c
vobject myAnim = GetObject("MyMod/data/anims/run.anm");
int frames = GetNumAnimFrames(myAnim);
Print("Total frames: " + frames);
```

---

## 🔍 Важные примечания
- **Кэширование:** Движок автоматически кэширует объекты, загруженные через `GetObject`.
- **Производительность:** Прямая работа с `vobject` — это "тяжелая" операция. Старайтесь загружать объекты один раз (например, в `EEInit`) и хранить ссылку, а не вызывать `GetObject` каждый кадр.
- **Типы файлов:** В основном используется для файлов `.xob` (модели) и `.anm` (анимации).
