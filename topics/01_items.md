# Items: Создание предметов

**Aliases:** item | предмет | объект | entity | ItemBase | EntityAI | создать предмет | create item
**Related:** [Config](topics/02_config.md) | [Economy](topics/05_economy.md)
**Examples:** [Minimal](../examples/minimal_item/) | [Edible](../examples/edible_item/) | [Weapon](../examples/weapon/)

---

## ⚡ 30-Second Summary

Чтобы создать предмет в DayZ:
1. **Создать класс** в `.c` файле, наследуемый от `ItemBase` (или потомка: `Edible_Base`, `Clothing`, и т.д.)
2. **Добавить описание** в `config.cpp` (класс в `CfgVehicles` с наследованием от базового класса)
3. **Настроить экономику** в `config.cpp` (параметры `nominal`, `min`, `max`, `lifetime`) или в отдельном `types.xml`

---

## 🎯 5-Minute Solution: Создать минимальный предмет

### Step 1: Создать класс предмета

**Файл:** `scripts/3_Game/Items/TestItem.c`

```c
class TestItem extends ItemBase
{
    override void OnInit()
    {
        // Код при инициализации предмета
    }
}
```

### Step 2: Добавить в config.cpp

**Файл:** `config.cpp`

```cpp
class CfgPatches
{
    class MyMod
    {
        units[] = {"TestItem"};
        weapons[] = {};
        requiredVersion = 0.1;
        requiredAddons[] = {};
    };
};

class CfgVehicles
{
    class ItemBase;
    class TestItem: ItemBase
    {
        scope = 2;  // 0 = скрыт, 1 = наследуется, 2 = публичный
        displayName = "Test Item";
        description = "Тестовый предмет";
        model = "\DZ\items\test_item.p3d";  // Модель (заглушка)
        picture = "";  // Иконка (пусто = используй set)
    };
};
```

### Step 3: (Опционально) Добавить параметры экономики

**Файл:** `config.cpp` (внутри класса TestItem)

```cpp
class TestItem: ItemBase
{
    // ... остальные свойства ...

    // Economy параметры
    nominal = 100;      // Желаемое количество на сервере
    min = 50;           // Минимальное количество
    max = 150;          // Максимальное количество
    restock = 0;        // Время респавна в секундах (0 = не респавнится)
    lifetime = 7200;    // Время жизни предмета в секундах (2 часа)
    quantity = 1;       // Количество в стаке
};
```

**Готово!** Минимальный предмет создан.

---

## 📚 Deep Dive

### Иерархия классов предметов

```
EntityAI (базовый объект для всего в мире)
└── InventoryItem (предмет с инвентарём)
    └── ItemBase (базовый предмет для всех предметов)
        ├── Edible_Base (еда: Apple, BakedBeans, etc.)
        ├── Consumable (расходники: Battery, Bandage, etc.)
        ├── Clothing (одежда: Shirt, Pants, etc.)
        ├── Weapon_Base (оружие: Rifle, Pistol, etc.)
        ├── Magazine (магазины для патронов)
        ├── AmmoBox (коробки с патронами)
        └── ... (многие другие)
```

**Ключевой принцип:** Выбирайте максимально специфичный базовый класс. Для еды используйте `Edible_Base`, для одежды - `Clothing`, и т.д.

---

### Типы предметов

| Тип | Базовый класс | Для чего | Примеры |
|-----|---------------|----------|---------|
| Обычный предмет | `ItemBase` | Предмет без специфики | Контейнер, декорация |
| Еда | `Edible_Base` | Съедобное (восстанавливает голод/жажду) | Apple, Meat, etc. |
| Расходник | `Consumable` | Расходуется при использовании | Battery, Bandage, Rope |
| Одежда | `Clothing` | Что можно надеть на игрока | Shirt, Jacket, Shoes |
| Оружие | `Weapon_Base` | Оружие (стреляет) | Rifle, Pistol, Bow |
| Магазин | `Magazine` | Магазин для патронов | Mag_AK_74, Mag_Magnum |
| Патроны | `AmmoBox` | Коробка с патронами | AmmoBox_762x39 |

---

### Свойства предмета (config.cpp)

#### Базовые свойства

```cpp
class TestItem: ItemBase
{
    // Доступность
    scope = 2;  // 0 = скрыт (не используется), 1 = наследуется, 2 = публичный

    // Отображение
    displayName = "$STR_test_item";  // Имя (из stringtable.csv или просто текст)
    description = "Описание предмета";  // Описание при наведении

    // Вес и размер
    weight = 100;        // Вес в граммах (0.1 кг)
    itemSize[] = {1, 2}; // Размер слота в инвентаре (1x2)

    // Модель
    model = "\DZ\items\test_item.p3d";  // Путь к 3D модели
    picture = "";  // Иконка (пусто = используй существующий set)
};
```

#### Экономика (spawn)

```cpp
class TestItem: ItemBase
{
    // Spawn параметры
    nominal = 100;      // Желаемое количество на сервере
    min = 50;           // Минимальное количество (не ниже)
    max = 150;          // Максимальное количество (не выше)
    restock = 0;        // Респавн в секундах (0 = не респавнится)
    lifetime = 7200;    // Время жизни в секундах (2 часа)
    quantity = 1;       // Количество в стаке (1 = стакаемый)
};
```

#### Здоровье и состояние

```cpp
class TestItem: ItemBase
{
    // Здоровье
    health = 100;        // Максимальное HP предмета
    damage = 0;          // Начальный урон (обычно 0)

    // Количество (для стакаемых предметов)
    quantity = 1;        // Начальное количество
    varQuantity = 1;     // Разрешить изменение количества (true/false)

    // Температура
    temperatureEnabled = 1;  // Влияет ли температура на предмет (1 = да)
    temperatureThreshold = 4;  // Температура замерзания (4°C)
};
```

---

### Slots и Attachments (слоты и присоединения)

**Что это:** Слот = место для присоединения другого предмета (например, батарейки в фонарике)

#### Добавить слоты в config.cpp

```cpp
class TestItem: ItemBase
{
    class Attachments
    {
        class Battery  // Слот для батарейки
        {
            slotName = "battery";       // Название слота
            slotType = "battery";       // Тип предмета (должен совпадать)
            attachmentAction = "";      // Действие при присоединении
        };
        class LightBulb  // Слот для лампочки
        {
            slotName = "lightbulb";
            slotType = "lightbulb";
            attachmentAction = "";
        };
    };
};
```

#### Работа с attachments в коде (.c файл)

```c
class TestItem extends ItemBase
{
    // При присоединении attachment
    override void OnAttach(Widget w, EntityAI parent)
    {
        super.OnAttach(w, parent);
        // Логика при присоединении
        // Например: включить фонарик если вставлена батарейка
    }

    // При отсоединении attachment
    override void OnDetach(EntityAI parent)
    {
        super.OnDetach(parent);
        // Логика при отсоединении
        // Например: выключить фонарик
    }

    // Проверить наличие attachment
    bool HasBattery()
    {
        return FindAttachmentBySlotName("battery") != null;
    }
};
```

---

### Lifecycle методы (жизненный цикл предмета)

```c
class TestItem extends ItemBase
{
    // OnInit - при создании предмета (спавн в мире)
    override void OnInit()
    {
        super.OnInit();
        // Инициализация переменных, настройка
    }

    // OnConsume - когда предмет "используется" (съеден, выпит и т.д.)
    override void OnConsume(PlayerBase player)
    {
        super.OnConsume(player);
        // Логика при потреблении
        // Для Edible: восстановить голод
    }

    // OnUpdate - каждый кадр (осторожно с производительностью!)
    override void OnUpdate(float delta_time)
    {
        super.OnUpdate(delta_time);
        // Обновление состояния
        // Например: уменьшение температуры
    }

    // OnPlacement - когда предмет размещается в мире (например, палатка)
    override void OnPlacementComplete( PlayerBase player )
    {
        super.OnPlacementComplete(player);
        // Логика после размещения
    }

    // AfterObjectLoad - после загрузки из сохранения
    override void AfterObjectLoad()
    {
        super.AfterObjectLoad();
        // Восстановление состояния
    }

    // SetActions - добавить действия в контекстное меню
    override void SetActions()
    {
        super.SetActions();
        AddAction(ActionToggleBodyHeat);
        AddAction(ActionEat);
        // ... другие действия
    }
};
```

---

## 💡 Examples

### Example 1: Minimal Item
→ [../examples/minimal_item/](../examples/minimal_item/)
Простейший предмет без функций

### Example 2: Edible Item (еда)
→ [../examples/edible_item/](../examples/edible_item/)
Еда, восстанавливает голод

### Example 3: Consumable Item (расходник)
→ [../examples/consumable_item/](../examples/consumable_item/)
Расходник (батарейка)

### Example 4: Clothing Item (одежда)
→ [../examples/clothing_item/](../examples/clothing_item/)
Одежда со слотами

### Example 5: Weapon (оружие)
→ [../examples/weapon/](../examples/weapon/)
Полноценное оружие

---

## 🔗 Related Topics

→ [Config: Наследование классов](topics/02_config.md#inheritance)
→ [Economy: Настройка спавна](topics/05_economy.md)
→ [Crafting: Рецепты](topics/06_crafting.md)
→ [Troubleshooting: Ошибки предметов](../troubleshooting/common_errors.md#item-errors)

---

## 🔍 Common Tasks

### Как создать Edible предмет (еду)?

1. Наследоваться от `Edible_Base` вместо `ItemBase`
2. Настроить параметры питания в config.cpp
3. Переопределить `OnConsume()` для кастомной логики

```c
class ApplePie extends Edible_Base
{
    override void OnConsume(PlayerBase player)
    {
        super.OnConsume(player);  // Стандартная логика (восстановление)
        // Своя логика здесь
    }
};
```

**Подробнее:** → topics/01_items.md#edible или [пример](../examples/edible_item/)

---

### Как добавить слоты для attachments?

1. Добавить секцию `class Attachments` в config.cpp
2. Определить слоты с `slotName` и `slotType`
3. Использовать `FindAttachmentBySlotName()` в коде

**Подробнее:** → topics/01_items.md#slots

---

### Как сделать предмет стакаемым (quantity)?

Например, патроны в коробке, батарейки в пачке:

```cpp
class AmmoBox extends ItemBase
{
    quantity = 20;       // 20 патронов в коробке
    varQuantity = 1;     // Разрешить изменение количества
};
```

**В коде:**
```c
// Уменьшить количество
AddQuantity(-1);

// Получить текущее количество
int count = GetQuantity();
```

---

### Как сделать предмет исчезающим после использования?

```c
class TestItem extends Edible_Base
{
    override void OnConsume(PlayerBase player)
    {
        super.OnConsume(player);

        // Удалить предмет после использования
        if (GetQuantity() <= 0)
        {
            DestroyItem();  // Или SetDamage(1.0)
        }
    }
};
```

---

### Как создать предмет с自定义 моделью?

1. Создать 3D модель в Object Builder
2. Добавить модель в `config.cpp`:
```cpp
model = "\MyMod\models\my_item.p3d";
```
3. Создать текстуры
4. Настроить материалы

**Примечание:** Моделирование выходит за рамки этой базы знаний.

---

### Как создать предмет, который можно носить в руках?

```c
class TestItem extends ItemBase
{
    override bool CanPutAsAttachment( EntityAI parent )
    {
        return true;  // Можно присоединить к другому предмету
    }

    override bool CanPutIntoHands( EntityAI player )
    {
        return true;  // Можно взять в руки
    }

    override bool CanPutInCargo( EntityAI owner )
    {
        return true;  // Можно положить в инвентарь
    }
};
```

---

### Как добавить действия в контекстное меню предмета?

```c
class TestItem extends ItemBase
{
    override void SetActions()
    {
        super.SetActions();

        // Добавить действия
        AddAction(ActionToggleBodyHeat);      // Согреться
        AddAction(ActionEat);                 // Есть
        AddAction(ActionCraftItem);           // Крафт
        AddAction(ActionDetach);              // Отсоединить
        // ... другие действия
    }
};
```

---

## 🚨 Troubleshooting

**Ошибка:** "Class not found"
- **Причина:** Класс не найден в config.cpp или неправильное наследование
- **Решение:** [troubleshooting/common_errors.md#class-not-found](../troubleshooting/common_errors.md#class-not-found)

**Ошибка:** "Item doesn't appear in game"
- **Причина:** `scope = 0` или не добавлен в economy
- **Решение:** [troubleshooting/common_errors.md#item-not-appear](../troubleshooting/common_errors.md#item-not-appear)

**Ошибка:** "Wrong attachment slot"
- **Причина:** Несоответствие `slotName` и `slotType`
- **Решение:** [troubleshooting/common_errors.md#wrong-slot](../troubleshooting/common_errors.md#wrong-slot)

---

## 📖 Additional Resources

- [DayZ-Samples/Test_Inputs](../samples/official/Test_Inputs/) - Примеры предметов
- [DayZ-Samples/Test_ClothingRetexture](../samples/official/Test_ClothingRetexture/) - Примеры одежды
- [raw_data/ItemBase.c](../raw_data/ItemBase.c) - Базовый класс предметов
