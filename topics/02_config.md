# Config: Конфигурация мода

**Aliases:** config | конфигурация | CfgPatches | CfgVehicles | config.cpp | настройка мода
**Related:** [Items](topics/01_items.md)
**Examples:** [Minimal Item](../examples/minimal_item/)

---

## ⚡ 30-Second Summary

`config.cpp` - главный файл конфигурации мода DayZ, который определяет:
1. **CfgPatches** - метаданные мода (название, версия, зависимости)
2. **CfgVehicles** - определения предметов/объектов (модели, свойства, наследование)
3. **CfgNonAIVehicles** (опционально) - другие объекты

Без `config.cpp` мод не загрузится в игру!

---

## 🎯 5-Minute Solution: Минимальный config.cpp

```cpp
class CfgPatches
{
    class MyMod
    {
        units[] = {"MyItem"};           // Предметы мода
        weapons[] = {};                  // Оружие мода (если есть)
        requiredVersion = 0.1;           // Минимальная версия
        requiredAddons[] = {
            "DZ_Data",                  // Зависимости от других модов
            "DZ_Items"
        };
    };
};

class CfgVehicles
{
    class ItemBase;                     // Базовый класс из игры
    class MyItem: ItemBase              // Ваш предмет
    {
        scope = 2;                      // 2 = публичный
        displayName = "My Item";        // Имя
        description = "Описание";        // Описание
        model = "\DZ\items\apple.p3d";  // Модель (заглушка)
    };
};
```

**Готово!** Это минимальная конфигурация для работы мода.

---

## 📚 Deep Dive

### Структура config.cpp

```
config.cpp
├── CfgPatches           (Обязательно!) Метаданные мода
│   └── class MyMod
│       ├── units[]          Предметы мода
│       ├── weapons[]        Оружие мода
│       ├── requiredVersion  Версия
│       └── requiredAddons[] Зависимости
│
└── CfgVehicles          (Обязательно!) Определения предметов
    ├── class ItemBase        (Базовый класс из игры)
    └── class MyItem: ItemBase (Ваш предмет)
        ├── scope
        ├── displayName
        ├── model
        └── ... свойства
```

---

### CfgPatches - метаданные мода

**Что это:** Секция, которая говорит игре "этот мод существует"

**Обязательно заполнить:**

```cpp
class CfgPatches
{
    class MyMod  // Название мода (должно совпадать с папкой мода)
    {
        // Предметы в моде (обязательно!)
        units[] = {
            "MyItem1",
            "MyItem2",
            "MyWeapon"
        };

        // Оружие в моде (если есть)
        weapons[] = {
            "MyWeapon"
        };

        // Минимальная версия мода
        requiredVersion = 0.1;

        // Зависимости от других модов
        requiredAddons[] = {
            "DZ_Data",      // База данных игры
            "DZ_Items",     // Предметы игры
            "DZ_Weapons"    // Оружие игры
        };
    };
};
```

**Важные моменты:**
- `units[]` - **обязательно** перечислить ВСЕ предметы мода
- `weapons[]` - если мод содержит оружие
- `requiredAddons[]` - только если используете ресурсы других модов

---

### CfgVehicles - определения предметов

**Что это:** Секция, где определяются все предметы/объекты мода

**Наследование:**

```cpp
class CfgVehicles
{
    // Сначала идут базовые классы (из игры)
    class ItemBase;
    class Edible_Base;
    class Clothing;

    // Потом ваши предметы (наследуются)
    class MyItem: ItemBase
    {
        // Свойства
    };

    class MyFood: Edible_Base
    {
        // Свойства еды
    };

    class MyShirt: Clothing
    {
        // Свойства одежды
    };
};
```

**Ключевое правило:** Сначала объявить базовый класс, потом наследоваться от него!

---

### Наследование классов

**Принцип:** Вы можете наследовать свойства от базового класса

```cpp
class CfgVehicles
{
    class ItemBase;  // Базовый (из игры)

    // MyItem наследует ВСЕ свойства ItemBase
    class MyItem: ItemBase
    {
        scope = 2;
        displayName = "My Item";
        // Остальные свойства наследуются от ItemBase
    };

    // MyItem2 наследует от MyItem (а не от ItemBase!)
    class MyItem2: MyItem
    {
        displayName = "My Item 2";  // Переопределяем только имя
        // Остальное наследуется от MyItem
    };
};
```

**Преимущества наследования:**
- Меньше кода (не копируете одинаковые свойства)
- Легче поддерживать (изменили базовый - везде применилось)

---

### Свойства предмета в CfgVehicles

#### Базовые свойства

```cpp
class MyItem: ItemBase
{
    // Обязательные свойства
    scope = 2;                      // 0=скрыт, 1=наследуется, 2=публичный
    displayName = "$STR_MY_ITEM";   // Имя (из stringtable или текст)
    description = "Описание предмета";
    model = "\MyMod\models\my_item.p3d";  // 3D модель

    // Размер и вес
    weight = 100;                   // Вес в граммах (0.1 кг)
    itemSize[] = {1, 1};            // Размер слота (1x1)

    // Экономика (spawn)
    nominal = 100;
    min = 50;
    max = 150;
    restock = 0;
    lifetime = 7200;
    quantity = 1;
};
```

#### Модель и геометрия

```cpp
class MyItem: ItemBase
{
    model = "\MyMod\models\my_item.p3d";

    // Геометрия (опционально)
    class Geometry
    {
        // Настройки коллизии
    };

    // Анимации (опционально)
    class AnimEvents
    {
        class Sound
        {
            soundSet = "Item_Drop_SoundSet";
        };
    };
};
```

---

### CfgNonAIVehicles (дополнительно)

**Когда использовать:** Для объектов, которые не являются предметами (здания, декорации)

```cpp
class CfgNonAIVehicles
{
    class Building;
    class MyBuilding: Building
    {
        scope = 2;
        displayName = "My Building";
        model = "\MyMod\buildings\my_building.p3d";
    };
};
```

---

## 💡 Полный пример config.cpp

### Пример простого мода с предметом

```cpp
class CfgPatches
{
    class TestMod
    {
        units[] = {"TestItem"};
        weapons[] = {};
        requiredVersion = 0.1;
        requiredAddons[] = {
            "DZ_Data",
            "DZ_Items"
        };
    };
};

class CfgVehicles
{
    class ItemBase;

    class TestItem: ItemBase
    {
        scope = 2;
        displayName = "Test Item";
        description = "Тестовый предмет для обучения";
        model = "\DZ\items\apple.p3d";

        weight = 100;
        itemSize[] = {1, 1};

        // Economy
        nominal = 100;
        min = 50;
        max = 150;
        restock = 0;
        lifetime = 7200;
        quantity = 1;
    };
};
```

**Смотрите полный пример:** → [examples/minimal_item/](../examples/minimal_item/)

---

### Пример мода с несколькими предметами

```cpp
class CfgPatches
{
    class FoodMod
    {
        units[] = {
            "ApplePie",
            "MeatSteak",
            "BreadLoaf"
        };
        weapons[] = {};
        requiredVersion = 0.1;
        requiredAddons[] = {
            "DZ_Data",
            "DZ_Items"
        };
    };
};

class CfgVehicles
{
    class ItemBase;
    class Edible_Base;

    // ApplePie
    class ApplePie: Edible_Base
    {
        scope = 2;
        displayName = "Apple Pie";
        model = "\DZ\items\apple.p3d";
        weight = 200;
        itemSize[] = {1, 2};
        nominal = 50;
        min = 20;
        max = 80;
    };

    // MeatSteak
    class MeatSteak: Edible_Base
    {
        scope = 2;
        displayName = "Meat Steak";
        model = "\DZ\items\meat_steak.p3d";
        weight = 300;
        itemSize[] = {1, 1};
        nominal = 30;
        min = 10;
        max = 50;
    };

    // BreadLoaf
    class BreadLoaf: Edible_Base
    {
        scope = 2;
        displayName = "Bread Loaf";
        model = "\DZ\items\bread.p3d";
        weight = 400;
        itemSize[] = {2, 2};
        nominal = 40;
        min = 15;
        max = 70;
    };
};
```

---

## 🔗 Related Topics

→ [Items: Создание предметов](topics/01_items.md)
→ [Economy: Настройка спавна](topics/05_economy.md)
→ [Troubleshooting: Ошибки config](../troubleshooting/common_errors.md#config-errors)

---

## 🔍 Common Tasks

### Как добавить новый предмет в config.cpp?

1. Добавить название предмета в `CfgPatches → units[]`
2. Создать класс в `CfgVehicles` с наследованием от базового класса
3. Настроить свойства предмета

**Подробнее:** → [Items: Создание предметов](topics/01_items.md)

---

### Как настроить зависимость от другого мода?

```cpp
class CfgPatches
{
    class MyMod
    {
        requiredAddons[] = {
            "DZ_Data",
            "OtherMod"  // Зависимость от OtherMod
        };
    };
};
```

**Важно:** Другой мод должен быть загружен ПЕРЕД вашим модом!

---

### Как использовать ресурсы другого мода?

```cpp
class MyItem: ItemBase
{
    model = "\OtherMod\models\item.p3d";  // Модель из другого мода
    picture = "\OtherMod\textures\icon.paa";  // Иконка из другого мода
};
```

---

### Как изменить свойства предмета из другого мода?

```cpp
class CfgPatches
{
    class MyMod
    {
        requiredAddons[] = {"OtherMod"};  // Зависимость
    };
};

class CfgVehicles
{
    class OtherItem;  // Из другого мода

    class ModdedOtherItem: OtherItem
    {
        weight = 500;  // Изменяем вес
        displayName = "Modified Other Item";  // Изменяем имя
    };
};
```

**Важно:** Используйте `modded class` в `.c` файле для изменения логики предмета!

---

## 🚨 Troubleshooting

**Ошибка:** "Config not found"
- **Решение:** Проверьте что `config.cpp` находится в корне мода

**Ошибка:** "Class not found in config"
- **Решение:** Проверьте правильность наследования (базовый класс должен быть объявлен)

**Ошибка:** "Mod doesn't load"
- **Решение:** Проверьте `CfgPatches → units[]` (все ли предметы перечислены)

**Подробнее:** → [troubleshooting/common_errors.md](../troubleshooting/common_errors.md)

---

## 📖 Additional Resources

- [raw_data/game_config.cpp](../raw_data/game_config.cpp) - Полный config игры
- [DayZ-Samples examples](../samples/official/) - Примеры конфигураций

---

## 🎓 Best Practices

1. **Всегда** заполняйте `CfgPatches → units[]`
2. **Используйте** наследование для уменьшения кода
3. **Проверяйте** правильность имён классов
4. **Используйте** stringtable.csv для локализации displayName
5. **Не забывайте** про `scope = 2` (иначе предмет не появится!)
