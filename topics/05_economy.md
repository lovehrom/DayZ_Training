# Economy: Настройка спавна

**Aliases:** economy | экономика | types.xml | spawn | спавн | nominal
**Related:** [Items](topics/01_items.md) | [Config](topics/02_config.md)

---

## ⚡ 30-Second Summary

Economy (экономика) - система спавна предметов в DayZ.

Чтобы настроить spawn предмета:
1. Добавить параметры economy в `config.cpp` ИЛИ создать `types.xml`
2. Настроить `nominal` (желаемое количество), `min`, `max`, `lifetime`
3. Настроить spawn zones (где спавнится)

---

## 🎯 5-Minute Solution: Добавить предмет в economy

### Вариант 1: Через config.cpp

```cpp
class CfgVehicles
{
    class ItemBase;
    class MyItem: ItemBase
    {
        // ... остальные свойства ...

        // Economy параметры
        nominal = 100;      // Желаемое количество на сервере
        min = 50;           // Минимальное
        max = 150;          // Максимальное
        restock = 0;        // Респавн (секунды, 0 = не респавнится)
        lifetime = 7200;    // Время жизни (секунды, 2 часа)
        quantity = 1;       // Количество в стаке
    };
};
```

### Вариант 2: Через types.xml

```xml
<type name="MyItem">
    <nominal>100</nominal>
    <min>50</min>
    <max>150</max>
    <restock>0</restock>
    <lifetime>7200</lifetime>
    <quantity>1</quantity>
</type>
```

---

## 📚 Deep Dive

### Параметры Economy

| Параметр | Описание | Пример |
|----------|----------|--------|
| `nominal` | Желаемое количество на сервере | `100` = 100 предметов |
| `min` | Минимальное количество | `50` = не ниже 50 |
| `max` | Максимальное количество | `150` = не выше 150 |
| `restock` | Время респавна (секунды) | `0` = не респавнится |
| `lifetime` | Время жизни предмета (секунды) | `7200` = 2 часа |
| `quantity` | Количество в стаке | `20` = 20 патронов |

---

### Как работает Economy

1. **Игра проверяет** текущее количество предметов
2. **Если < min:** Спавнит предметы до `nominal`
3. **Если между min и max:** Ничего не делает
4. **Если > max:** Удаляет лишние предметы (по `lifetime`)

---

### Spawn Zones (зоны спавна)

**Что это:** Места, где спавнится предмет

**Примеры зон:**
- `Village` - Деревни
- `City` - Города
- `Military` - Военные базы
- `Coast` - Побережье
- `Wilderness` - Дикая природа

**Настроить зону спавна:**

```cpp
class MyItem: ItemBase
{
    // ... параметры economy ...

    // Зона спавна
    class SpawnZones
    {
        Village[] = {1, 1};  // Спавнится в деревнях (приоритет 1)
        City[] = {0.5, 1};   // Реже в городах (приоритет 0.5)
    };
};
```

---

### Categories (категории)

**Что это:** Группы предметов для spawn

**Примеры категорий:**
- `Food` - Еда
- `Drinks` - Напитки
- `Weapons` - Оружие
- `Clothes` - Одежда

**Добавить в категорию:**

```cpp
class MyItem: ItemBase
{
    category = "Food";  // Категория еды
};
```

---

## 💡 Examples

### Example 1: Еда в домах

```cpp
class ApplePie: Edible_Base
{
    // Спавнится в домах
    nominal = 100;
    min = 50;
    max = 150;
    restock = 0;
    lifetime = 7200;

    // Зоны спавна
    class SpawnZones
    {
        Village[] = {1, 1};
        City[] = {0.5, 1};
    };

    // Категория
    category = "Food";
};
```

### Example 2: Оружие на военных базах

```cpp
class MyRifle: Weapon_Base
{
    // Редкий спавн
    nominal = 10;
    min = 5;
    max = 15;
    restock = 1800;  // Респавн каждые 30 минут
    lifetime = 10800;  // 3 часа

    // Только на военных базах
    class SpawnZones
    {
        Military[] = {1, 1};
    };

    category = "Weapons";
};
```

---

## 🔗 Related Topics

→ [Items: Создание предметов](topics/01_items.md)
→ [Config: Конфигурация](topics/02_config.md)

---

## 🔍 Common Tasks

### Как сделать предмет редким?

```cpp
nominal = 5;   // Мало на сервере
min = 2;
max = 8;
```

---

### Как сделать предмет частым?

```cpp
nominal = 500;  // Много на сервере
min = 300;
max = 700;
```

---

### Как сделать предмет не исчезающим?

```cpp
lifetime = -1;  // Бесконечная жизнь
restock = 0;    // Не респавнится
```

---

### Как настроить спавн только в определённых местах?

```cpp
class SpawnZones
{
    Village[] = {0, 0};      // НЕ спавнится в деревнях
    Military[] = {1, 1};     // Спавнится на военных базах
    Coast[] = {0.5, 1};      // Реже на побережье
};
```

---

## 🚨 Troubleshooting

**Проблема:** Предмет не спавнится
- **Решение:** Проверьте `nominal > 0` и предмет добавлен в economy

**Проблема:** Предмет спавнится слишком часто
- **Решение:** Уменьшите `nominal`

**Проблема:** Предмет исчезает слишком быстро
- **Решение:** Увеличьте `lifetime`

---

## 📖 Additional Resources

- [DayZ-Samples economy examples](../samples/official/) - Примеры economy

---

## 🎓 Best Practices

1. **Устанавливайте** `nominal` реалистично (не слишком много/мало)
2. **Используйте** `restock` для редких предметов
3. **Настраивайте** `lifetime` для баланса
4. **Используйте** зоны спавна для контроля
5. **Тестируйте** баланс на сервере
