# Network: RPC и синхронизация

**Aliases:** network | rpc | синхронизация | server | client | multiplayer
**Related:** [PlayerBase](topics/04_playerbase.md)

---

## ⚡ 30-Second Summary

RPC (Remote Procedure Call) - вызов функции на удалённой машине (Server ↔ Client).

Для сетевого взаимодействия:
1. Создать RPC методы с `[RpcEnum()]`
2. Вызвать через `RPC()` на сервере ИЛИ клиенте
3. Использовать `Synchronized` для синхронизации переменных

---

## 🎯 5-Minute Solution: Простой RPC

### Шаг 1: Создать RPC методы

```c
class MyNetworkClass
{
    // RPC на сервере
    [RpcEnum RpcServer]
    void ServerMethod(CallType type, ParamsReadContext ctx)
    {
        // Выполняется на сервере
        Print("Вызвано на сервере!");
    }

    // RPC на клиенте
    [RpcEnum RpcClient]
    void ClientMethod(CallType type, ParamsReadContext ctx)
    {
        // Выполняется на клиенте
        Print("Вызвано на клиенте!");
    }
};
```

### Шаг 2: Вызвать RPC

```c
// Вызвать на сервере (с клиента)
myNetworkObject.ServerMethod();

// Вызвать на клиенте (с сервера)
myNetworkObject.ClientMethod();
```

---

## 📚 Deep Dive

### Типы RPC

| Тип | Откуда | Куда | Когда использовать |
|-----|--------|------|-------------------|
| `RpcServer` | Сервер | Сервер | Логика только на сервере |
| `RpcClient` | Клиент | Клиент | Визуальные эффекты |
| `Server` | Клиент → Сервер | Отправить данные на сервер | Действия игрока |
| `Client` | Сервер → Клиент | Отправить на клиент | Обновление состояния |

---

### Synchronized переменные

**Что это:** Переменные, которые автоматически синхронизируются Server ↔ Client

```c
class MySyncedClass
{
    // Переменная синхронизируется автоматически
    [Synchronized()]
    protected float m_MyVariable = 0.0;

    void SetMyVariable(float value)
    {
        m_MyVariable = value;  // Автоматически синхронизируется на всех клиентах
    }

    float GetMyVariable()
    {
        return m_MyVariable;
    }
};
```

---

## 💡 Examples

### Example 1: Группа игрока

→ [examples/group_system/] (пока не создан)

### Example 2: Синхронизация здоровья

```c
modded class PlayerBase
{
    [Synchronized()]
    protected float m_MyHealth = 100.0;

    [RpcEnum Server]
    void TakeDamageServer(float damage)
    {
        m_MyHealth -= damage;
    }

    [RpcEnum Client]
    void ShowDamageEffectClient(float damage)
    {
        // Показать эффект урона на клиенте
    }
};
```

---

## 🔗 Related Topics

→ [PlayerBase: Модификация игрока](topics/04_playerbase.md)

---

## 🔍 Common Tasks

### Как отправить данные с клиента на сервер?

```c
// На клиенте
player.RPCSingleParam("TakeDamageServer", new Param1<float>(50.0));
```

---

### Как отправить данные с сервера на клиент?

```c
// На сервере
player.RPCSingleParam("ShowDamageEffectClient", new Param1<float>(50.0));
```

---

## 🚨 Troubleshooting

**Проблема:** RPC не вызывается
- **Решение:** Проверьте что объект является Network-объектом

**Проблема:** Переменная не синхронизируется
- **Решение:** Добавьте `[Synchronized()]` атрибут

---

## 📖 Additional Resources

- [DayZ Network documentation](../external_docs/) - Если доступна

---

## 🎓 Best Practices

1. **Валидируйте** данные на сервере (никогда не доверяйте клиенту)
2. **Минимизируйте** RPC вызовы (это дорого)
3. **Используйте** `Synchronized` для автоматической синхронизации
4. **Разделяйте** логику на серверную и клиентскую
5. **Тестируйте** в мультиплеере
