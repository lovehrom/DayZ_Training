# UI: Интерфейс и HUD

**Aliases:** ui | интерфейс | hud | widget | виджет | меню

---

## ⚡ 30-Second Summary

UI в DayZ создаётся через:
1. **Widgets** - компоненты интерфейса (кнопки, текст, и т.д.)
2. **Layouts** - структура интерфейса
3. **ScriptedWidgets** - динамические виджеты из кода

Для простого UI используйте ScriptedWidget.

---

## 🎯 5-Minute Solution: Создать текстовый виджет

**Файл:** `scripts/3_Game/MyUIWidget.c`

```c
class MyUIWidget: ScriptedWidget
{
    override void OnInit()
    {
        // Создать текст
        TextWidget text = TextWidget.GetWidget();

        if (text)
        {
            text.SetText("Hello World!");
            text.SetColor(ARGB(255, 255, 255, 255));
        }
    }
};
```

**Показать виджет:**

```c
MyUIWidget widget = new MyUIWidget();
widget.Show();
```

---

## 📚 Deep Dive

### Типы Widgets

| Тип | Описание | Пример |
|-----|----------|--------|
| `TextWidget` | Текст | Надписи, лейблы |
| `ImageWidget` | Изображение | Иконки, картинки |
| `ButtonWidget` | Кнопка | Интерактивные элементы |
| `GridWidget` | Сетка | Инвентарь |
| `ScrollWidget` | Скролл | Списки |

---

## 💡 Examples

### Example: HUD индикатор здоровья

```c
class HealthHud: ScriptedWidget
{
    private TextWidget m_HealthText;

    override void OnInit()
    {
        m_HealthText = TextWidget.GetWidget();
    }

    override void OnUpdate(float delta_time)
    {
        PlayerBase player = GetGame().GetPlayer();
        if (player)
        {
            float health = player.GetHealth();
            m_HealthText.SetText("Health: " + health.ToString());
        }
    }
};
```

---

## 🔗 Related Topics

→ [PlayerBase: Модификация игрока](topics/04_playerbase.md)

---

## 🚨 Troubleshooting

**Проблема:** Виджет не отображается
- **Решение:** Проверьте что `.Show()` вызван

---

## 📖 Additional Resources

- [DayZ UI documentation](../external_docs/) - Если доступна

---

## 🎓 Best Practices

1. **Минимизируйте** UI элементов (это влияет на FPS)
2. **Используйте** слои для z-order
3. **Тестируйте** на разных разрешениях
4. **Скрывайте** UI когда не нужен (`.Hide()`)
