# AI: Зомби и животные

**Aliases:** ai | zombie | зомби | animal | животное | поведение

---

## ⚡ 30-Second Summary

AI в DayZ (зомби, животные) модифицируется через наследование от базовых классов:
- `ZombieBase` - зомби
- `AnimalBase` - животные

Для кастомного AI:
1. Создать класс, наследуемый от `ZombieBase` ИЛИ `AnimalBase`
2. Переопределить поведение (damage, sounds, loot)
3. Настроить spawn в economy

---

## 🎯 5-Minute Solution: Создать кастомного зомби

**Файл:** `scripts/4_World/Entities/AI/NurseZombie.c`

```c
class NurseZombie extends ZombieBase
{
    override void OnInit()
    {
        super.OnInit();

        // Больше HP
        SetHealth(200);  // Вместо 100

        // Медленнее
        GetMovementSpeed().m_walkSpeed = 0.8;
    }

    override void OnDeath()
    {
        super.OnDeath();

        // Специальный лут
        SpawnItem("Bandage");
        SpawnItem("Morphine");
    }
};
```

**config.cpp:**

```cpp
class CfgVehicles
{
    class ZombieBase;
    class NurseZombie: ZombieBase
    {
        scope = 2;
        displayName = "Nurse Zombie";
        model = "\DZ\animals\zombie_worker.p3d";

        // Spawn только в больницах
        nominal = 20;
        min = 10;
        max = 30;

        class SpawnZones
        {
            Medical[] = {1, 1};
        };
    };
};
```

---

## 📚 Deep Dive

### ZombieBase - базовый класс зомби

**Основные методы:**
- `OnInit()` - Инициализация
- `OnAttack()` - При атаке
- `OnDamage()` - При получении урона
- `OnDeath()` - При смерти
- `GetMovementSpeed()` - Скорость движения

---

### AnimalBase - базовый класс животных

**Основные методы:**
- `OnInit()` - Инициализация
- `OnAttack()` - При атаке
- `OnFlee()` - При бегстве

---

## 💡 Examples

### Example 1: Зомби с особым лутом

```c
class SoldierZombie extends ZombieBase
{
    override void OnDeath()
    {
        super.OnDeath();

        // Спавнить оружие
        SpawnItem("Mag_AK_74");
        SpawnItem("AmmoBox_762x39_20B");
    }
};
```

### Example 2: Быстрое животное

```c
class FastDeer extends AnimalBase
{
    override void OnInit()
    {
        super.OnInit();

        // Быстрее бегает
        GetMovementSpeed().m_runSpeed = 2.0;
    }
};
```

---

## 🔗 Related Topics

→ [Economy: Настройка спавна](topics/05_economy.md)

---

## 🔍 Common Tasks

### Как изменить HP зомби?

```c
override void OnInit()
{
    super.OnInit();
    SetHealth(200);  // 200 HP
}
```

---

### Как изменить урон зомби?

```c
modded class ZombieBase
{
    override void OnAttack()
    {
        super.OnAttack();

        // Дополнительный урон
        PlayerBase player = PlayerBase.Cast(target);
        if (player)
            player.AddHealth("", "", -20);  // +20 урона
    }
};
```

---

### Как добавить кастомный звук?

```c
modded class ZombieBase
{
    override void OnInit()
    {
        super.OnInit();

        // Установить звук
        SoundSound("My_SoundSet");
    }
};
```

---

## 🚨 Troubleshooting

**Проблема:** Зомби не спавнится
- **Решение:** Проверьте `nominal > 0` в economy

**Проблема:** Зомби не использует кастомное поведение
- **Решение:** Проверьте что класс правильно указан в config.cpp

---

## 📖 Additional Resources

- [raw_data/ZombieBase.c](../raw_data/) - Если доступен
- [DayZ-Samples examples](../samples/official/) - Примеры AI

---

## 🎓 Best Practices

1. **Не делайте** слишком сложное AI (это влияет на сервер)
2. **Используйте** существующие анимации
3. **Тестируйте** лут на сервере
4. **Балансируйте** HP и damage
