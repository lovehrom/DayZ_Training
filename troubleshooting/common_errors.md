# Common Errors - Типичные ошибки

**Самые частые ошибки при создании модов для DayZ и их решения.**

---

## Class not found

**Сообщение:** `Cannot open object 'MyItem' - Class not found`

**Причина:** Класс не найден в config.cpp ИЛИ неправильное наследование

**Решение:**

1. **Проверьте что класс есть в config.cpp:**
```cpp
class CfgVehicles
{
    class ItemBase;  // Базовый класс объявлен
    class MyItem: ItemBase  // Ваш класс
    {
        // ...
    };
};
```

2. **Проверьте правильность наследования:**
```cpp
// ❌ НЕПРАВИЛЬНО (нет базового класса):
class MyItem
{
}

// ✅ ПРАВИЛЬНО (есть наследование):
class MyItem: ItemBase
{
}
```

3. **Проверьте CfgPatches:**
```cpp
class CfgPatches
{
    class MyMod
    {
        units[] = {"MyItem"};  // Предмет перечислен
    };
};
```

---

## Item doesn't appear in game

**Сообщение:** Предмет не появляется в игре

**Причина:** `scope = 0` ИЛИ не добавлен в economy

**Решение:**

1. **Проверьте scope:**
```cpp
class MyItem: ItemBase
{
    scope = 2;  // Должно быть 2 (не 0 или 1)
}
```

2. **Добавьте в economy:**
```cpp
class MyItem: ItemBase
{
    nominal = 100;  // Добавить это
    min = 50;
    max = 150;
}
```

3. **Проверьте что мод загружается:**
- Запустите игру
- Проверьте логи (script.log, RPT.log)
- Найдите строку "MyMod loaded"

---

## Config.cpp not found

**Сообщение:** `Cannot open config.cpp`

**Причина:** config.cpp не найден в корне мода

**Решение:**

1. **Убедитесь что config.cpp в корне:**
```
@MyMod/
├── config.cpp  ← Должен быть здесь!
├── scripts/
└── ...
```

2. **Проверьте название файла:**
- `config.cpp` ✅
- `Config.cpp` ❌ (Windows - не критично, но лучше lowercase)
- `config.txt` ❌ (должно быть .cpp)

---

## Script compile error

**Сообщение:** `Script compile error`

**Причина:** Синтаксическая ошибка в `.c` файле

**Решение:**

1. **Проверьте script.log:**
```bash
# Путь к логам
# Windows: C:\Users\[USERNAME]\Documents\DayZ\scripts.log
```

2. **Найдите строку с ошибкой:**
```
File: scripts/3_Game/Items/MyItem.c, Line: 15
Error: Missing semicolon
```

3. **Исправьте ошибку:**
```c
// ❌ НЕПРАВИЛЬНО:
class MyItem extends ItemBase
{  // <-- Пропущена скобка

// ✅ ПРАВИЛЬНО:
class MyItem extends ItemBase
{
```

---

## Mod doesn't load

**Сообщение:** Мод не загружается

**Причина:** Ошибка в config.cpp ИЛИ не загружены зависимости

**Решение:**

1. **Проверьте CfgPatches:**
```cpp
class CfgPatches
{
    class MyMod
    {
        requiredAddons[] = {
            "DZ_Data",  // Зависимости должны быть загружены
            "DZ_Items"
        };
    };
};
```

2. **Проверьте порядок загрузки модов:**
- Убедитесь что зависимые моды загружены ПЕРЕД вашим модом

3. **Проверьте логи:**
- script.log - ошибки скриптов
- RPT.log - общие ошибки

---

## Wrong attachment slot

**Сообщение:** Attachment не подходит к слоту

**Причина:** `slotName` и `slotType` не совпадают

**Решение:**

1. **Проверьте что slotName == slotType:**
```cpp
class MyItem: ItemBase
{
    class Attachments
    {
        class Battery
        {
            slotName = "battery";  // Должно совпадать
            slotType = "battery";  // Должно совпадать
        };
    };
};
```

2. **Проверьте что тип attachment верен:**
```cpp
// Батарейка должна иметь тип "battery"
class Battery: ItemBase
{
    // ...
}
```

---

## Stack level too deep

**Сообщение:** `Stack level too deep`

**Причина:** Бесконечная рекурсия (забыли `super()`)

**Решение:**

1. **Всегда вызывайте super():**
```c
modded class PlayerBase
{
    override void OnInit()
    {
        super.OnInit();  // ← ВСЕГДА вызывайте!

        // Ваш код
    }
};
```

2. **Проверьте что нет циклических вызовов:**
```c
// ❌ НЕПРАВИЛЬНО (бесконечная рекурсия):
override void OnInit()
{
    OnInit();  // Вызывает сам себя!
}

// ✅ ПРАВИЛЬНО:
override void OnInit()
{
    super.OnInit();  // Вызывает родительский класс
}
```

---

## Model not found

**Сообщение:** `Cannot load model 'MyModel.p3d'`

**Причина:** Модель не найдена ИЛИ неправильный путь

**Решение:**

1. **Используйте заглушку для теста:**
```cpp
class MyItem: ItemBase
{
    model = "\DZ\items\apple.p3d";  // Используйте существующую модель
}
```

2. **Добавьте свою модель:**
```cpp
model = "\MyMod\models\my_item.p3d";  // Правильный путь
```

3. **Проверьте что модель существует:**
- Путь должен быть правильным
- Модель должна быть `.p3d` форматом

---

## Out of memory

**Сообщение:** `Out of memory`

**Причина:** Утечка памяти ИЛИ слишком много объектов

**Решение:**

1. **Проверьте утечки памяти:**
```c
// ❌ НЕПРАВИЛЬНО (утечка памяти):
override void OnUpdate(float delta_time)
{
    MyObject obj = new MyObject();  // Создаёт КАЖДЫЙ кадр!
}

// ✅ ПРАВИЛЬНО:
private MyObject m_Obj;

override void OnInit()
{
    m_Obj = new MyObject();  // Создаётся один раз
}
```

2. **Используйте таймеры вместо OnUpdate:**
```c
// Не выполняйте тяжёлые действия каждый кадр
// Используйте таймеры (каждые N секунд)
```

---

## Action not appearing in menu

**Сообщение:** Действие не появляется в контекстном меню

**Причина:** `ActionCondition()` возвращает `false`

**Решение:**

1. **Проверьте ActionCondition():**
```c
override bool ActionCondition(PlayerBase player, ActionTarget target)
{
    // Добавьте Debug лог
    Print("ActionCondition called");

    if (!player.IsAlive()) return false;

    return true;  // Должно возвращать true
}
```

2. **Проверьте SetActions():**
```c
modded class MyItem extends ItemBase
{
    override void SetActions()
    {
        super.SetActions();
        AddAction(ActionMyAction);  // Действие добавлено
    }
};
```

---

## Performance issues (low FPS)

**Сообщение:** Низкий FPS после добавления мода

**Причина:** Слишком много логики в `OnUpdate()`

**Решение:**

1. **Не используйте OnUpdate() для тяжёлых вычислений:**
```c
// ❌ НЕПРАВИЛЬНО:
override void OnUpdate(float delta_time)
{
    // Тяжёлое вычисление каждый кадр (60 раз в секунду!)
    for (int i = 0; i < 1000; i++)
    {
        // ...
    }
}

// ✅ ПРАВИЛЬНО:
private float m_Timer = 0;

override void OnUpdate(float delta_time)
{
    m_Timer += delta_time;
    if (m_Timer >= 1.0)  // Каждую секунду (а не каждый кадр)
    {
        m_Timer = 0;
        // Выполнить вычисление
    }
}
```

2. **Минимизируйте использование OnFrame():**
- `OnFrame()` вызывается ПОСЛЕ `OnUpdate()`
- Используйте только для визуальных эффектов

---

## 🔗 Related Topics

→ [Debug Checklist](debug_checklist.md)

---

## 📖 Additional Resources

- [raw_data/game_config.cpp](../raw_data/game_config.cpp) - Примеры из игры
- [DayZ-Samples examples](../samples/official/) - Рабочие примеры

---

## 🎓 Best Practices

1. **Всегда** проверяйте script.log при ошибках
2. **Всегда** вызывайте `super()` в modded classes
3. **Используйте** таймеры вместо OnUpdate для тяжёлой логики
4. **Тестируйте** мод на чистом сервере
5. **Начинайте** с простого и усложняйте постепенно
