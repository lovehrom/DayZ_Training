# Debug Checklist - Чеклист отладки

**Пошаговая инструкция для поиска и исправления ошибок в модах DayZ.**

---

## 🚀 Phase 0: Перед началом

### Проверить окружение
- [ ] **DayZ сервер запущен?**
  - Проверьте процесс `DayZServer.exe` в диспетчере задач
- [ ] **Мод загружается?**
  - Проверьте параметр запуска: `-mod=@MyMod`
  - Проверьте что мод находится в правильной папке
- [ ] **Логи доступны?**
  - script.log: `C:\Users\[USERNAME]\Documents\DayZ\scripts.log`
  - RPT.log: `C:\Users\[USERNAME]\Documents\DayZ\DayZ_x64.RPT`
- [ ] **config.cpp существует?**
  - Файл должен быть в корне мода: `@MyMod/config.cpp`

---

## 🔧 Phase 1: Проверка мода

### Шаг 1.1: Проверить CfgPatches
```cpp
class CfgPatches
{
    class MyMod
    {
        units[] = {"MyItem"};        // ← Предмет перечислен
        weapons[] = {};
        requiredVersion = 0.1;
        requiredAddons[] = {
            "DZ_Data",               // ← Зависимости указаны
            "DZ_Items"
        };
    };
};
```

**Чеклист:**
- [ ] Класс мода указан (`MyMod`)
- [ ] Предметы перечислены в `units[]`
- [ ] Зависимости указаны в `requiredAddons[]`
- [ ] `requiredVersion` указан

**Если ошибка:**
→ [Config.cpp syntax error](common_errors.md#config-error)

### Шаг 1.2: Проверить загрузку мода
**Действия:**
1. Запустите сервер
2. Откройте `script.log`
3. Найдите строку: `MyMod loaded`

**Если не найдено:**
- [ ] Проверьте путь к моду
- [ ] Проверьте параметр `-mod=`
- [ ] Проверьте синтаксис config.cpp

**Если ошибка при загрузке:**
→ [Mod doesn't load](common_errors.md#mod-doesnt-load)

---

## 📦 Phase 2: Проверка предмета

### Шаг 2.1: Проверить CfgVehicles
```cpp
class CfgVehicles
{
    class ItemBase;                  // ← Базовый класс объявлен
    class MyItem: ItemBase           // ← Правильное наследование
    {
        scope = 2;                   // ← Должно быть 2
        displayName = "My Item";
        model = "\DZ\items\apple.p3d";  // ← Модель существует
    };
};
```

**Чеклист:**
- [ ] Базовый класс объявлен (`class ItemBase;`)
- [ ] Правильное наследование (`class MyItem: ItemBase`)
- [ ] `scope = 2` (не 0 или 1)
- [ ] Модель существует (или заглушка)

**Если ошибка:**
→ [Class not found](common_errors.md#class-not-found)

### Шаг 2.2: Проверить скрипт предмета
**Файл:** `scripts/3_Game/Items/MyItem.c`

```c
class MyItem extends ItemBase        // ← extends, не :
{
    override void OnInit()
    {
        super.OnInit();              // ← super() вызван
    }
}
```

**Чеклист:**
- [ ] Правильное наследование (`extends ItemBase`)
- [ ] `super()` вызывается в override методах
- [ ] Нет синтаксических ошибок (скобки, точки с запятой)
- [ ] Файл `.c` (не `.cpp`)

**Если ошибка:**
→ [Script compile error](common_errors.md#script-compile-error)

### Шаг 2.3: Проверить economy (если предмет не спавнится)
```cpp
class MyItem: ItemBase
{
    nominal = 100;                   // ← Добавлено
    min = 50;
    max = 150;
    restock = 0;
    lifetime = 7200;
};
```

**Чеклист:**
- [ ] `nominal` указан (> 0)
- [ ] `min` и `max` указаны
- [ ] `lifetime` указан

**Если предмет не появляется:**
→ [Item doesn't appear in game](common_errors.md#item-not-appear)

---

## 🧪 Phase 3: Тестирование

### Шаг 3.1: Прямой спавн через консоль
**Действия:**
1. Зайдите на сервер
2. Откройте консоль (клавиша по умолчанию: `/`)
3. Введите команду:
```
_test = "MyItem"_spawn
```

**Ожидаемый результат:** Предмет появляется перед игроком

**Если не работает:**
- [ ] Проверьте что класс существует в config.cpp
- [ ] Проверьте `scope = 2`
- [ ] Проверьте script.log на ошибки

### Шаг 3.2: Поиск в файле types.xml
**Действия:**
1. Откройте `types.xml` (находится в `mission/ServerProfile/`)
2. Найдите ваш предмет:
```xml
type name="MyItem">
    <nominal>100</nominal>
    <min>50</min>
    <max>150</max>
    <lifetime>7200</lifetime>
    <restock>0</restock>
</type>
```

**Если не найдено:**
→ Добавьте предмет в types.xml

---

## 🔍 Phase 4: Поиск ошибок в логах

### Шаг 4.1: Проверить script.log
**Расположение:** `C:\Users\[USERNAME]\Documents\DayZ\scripts.log`

**Ищите:**
- `Error` → ошибки
- `Warning` → предупреждения
- `Cannot open` → файл не найден
- `Class not found` → класс не найден
- `Script compile error` → синтаксическая ошибка

**Пример ошибки:**
```
File: scripts/3_Game/Items/MyItem.c, Line: 15
Error: Missing semicolon
```

**Решение:** Исправьте строку 15 в файле

### Шаг 4.2: Проверить RPT.log
**Расположение:** `C:\Users\[USERNAME]\Documents\DayZ\DayZ_x64.RPT`

**Ищите:**
- `ErrorMessage` → сообщения об ошибках
- `Out of memory` → утечка памяти
- `Cannot load model` → модель не найдена

**Если найдены ошибки:**
→ Соответствующая секция в [common_errors.md](common_errors.md)

---

## 🎯 Phase 5: Debug действий (Actions)

### Шаг 5.1: Проверить ActionCondition
```c
override bool ActionCondition(PlayerBase player, ActionTarget target)
{
    // Добавьте debug лог
    Print("ActionCondition called");

    if (!player.IsAlive()) return false;

    return true;  // ← Должно возвращать true
}
```

**Чеклист:**
- [ ] Добавлен `Print()` для отладки
- [ ] Метод возвращает `true` при правильных условиях
- [ ] Условия проверяются корректно

**Если действие не появляется:**
→ [Action not appearing in menu](common_errors.md#action-not-appearing)

### Шаг 5.2: Проверить SetActions()
```c
modded class MyItem extends ItemBase
{
    override void SetActions()
    {
        super.SetActions();
        AddAction(ActionMyAction);  // ← Действие добавлено
    }
};
```

**Чеклист:**
- [ ] `super.SetActions()` вызван
- [ ] `AddAction()` вызван с правильным классом
- [ ] Action класс существует

---

## 📊 Phase 6: Performance Debug

### Шаг 6.1: Проверить OnUpdate()
```c
override void OnUpdate(float delta_time)
{
    // ❌ НЕПРАВИЛЬНО (создаёт объект каждый кадр)
    MyObject obj = new MyObject();

    // ✅ ПРАВИЛЬНО (использует таймер)
    m_Timer += delta_time;
    if (m_Timer >= 1.0)  // Каждую секунду
    {
        m_Timer = 0;
        // Выполнить действие
    }
}
```

**Если низкий FPS:**
→ [Performance issues](common_errors.md#performance-issues)

### Шаг 6.2: Проверить утечки памяти
**Симптомы:**
- FPS падает со временем
- "Out of memory" в логах

**Решение:**
- [ ] Не создавайте объекты в `OnUpdate()`
- [ ] Используйте таймеры вместо постоянных проверок
- [ ] Удаляйте ненужные объекты

---

## ✅ Phase 7: Final Check

### Перед завершением:
- [ ] Мод загружается без ошибок
- [ ] Предмет спавнится через консоль
- [ ] Логи не содержат ошибок
- [ ] Действия выполняются корректно
- [ ] FPS стабилен

### Если всё работает:
→ Мод готов к использованию!

### Если есть проблемы:
→ Вернитесь к соответствующей фазе и исправьте

---

## 🔧 Быстрый поиск проблем

| Симптом | Вероятная причина | Решение |
|--------|-------------------|---------|
| Мод не загружается | Ошибка в config.cpp | Проверьте синтаксис |
| Предмет не спавнится | `scope = 0` ИЛИ не в economy | Измените `scope = 2` |
| Ошибка "Class not found" | Класс не в config.cpp | Добавьте в CfgVehicles |
| Действие не в меню | `ActionCondition()` возвращает `false` | Проверьте условия |
| Низкий FPS | Утечка памяти в `OnUpdate()` | Используйте таймеры |
| Ошибка компиляции | Синтаксическая ошибка в `.c` | Проверьте скобки, `;` |

---

## 📖 Дополнительные ресурсы

→ [common_errors.md](common_errors.md) - Типичные ошибки
→ [../../topics/02_config.md](../topics/02_config.md) - Config.cpp
→ [../../topics/01_items.md](../topics/01_items.md) - Создание предметов

---

**Последнее обновление:** 2025-01-16
